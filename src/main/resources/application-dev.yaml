spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost:3306}/${DB_SCHEMA:store}?createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:password!}
  jpa:
    show-sql: true

websiteUrl: http://localhost:3000