spring:
  application:
    name: store
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: DmaFiel@7221997
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}